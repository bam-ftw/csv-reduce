{
  "name": "csv-reduce",
  "version": "1.0.1",
  "description": "Application for generating CSV data with subset of columns from a CSV file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [ "csv", "reduce", "subset", "columns" ],
  "author": "Michal Molawka",
  "license": "ISC",
  "dependencies": {
    "readline": "^1.3.0",
    "yargs": "^12.0.2"
	},
	"preferGlobal": true,
	"bin": {
		"csv-reduce": "./bin/csv-reduce.js"
	}
}
